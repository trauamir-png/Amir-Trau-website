# Google Tag Manager container ID (e.g. GTM-XXXXXXX)
# Leave empty locally if you don't need tracking during development.
NEXT_PUBLIC_GTM_ID=

# Resend API key, used by app/api/contact/route.ts to send contact-form emails
RESEND_API_KEY=
